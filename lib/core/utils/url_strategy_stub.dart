/// No-op URL strategy for non-web platforms.
///
/// `package:flutter_web_plugins` only compiles for the web, so `main.dart`
/// conditionally imports this stub everywhere else.
void configureUrlStrategy() {}
